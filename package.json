{
  "name": "disabler",
  "version": "2.0.1",
  "description": "Disable buttons, preventing duplicate form submissions.",
  "author": "Gary Green",
  "main": "src/disabler.js",
  "scripts": {
    "build": "rollup -v && rollup src/disabler.js --file dist/disabler.js --format iife --name=Disabler",
    "dist": "npm run build && uglifyjs dist/disabler.js -o dist/disabler.min.js"
  },
  "dependencies": {},
  "devDependencies": {
    "rollup": "^0.63.4",
    "uglify-js": "^3.4.6"
  }
}
